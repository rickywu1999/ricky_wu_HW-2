#include <stdio.h>
#include <string.h>

struct node { int i; struct node *next };

int print_list(*int a){
  
}

int main(){
  struct node *node1;
  struct node *node2;
  struct node *node3;
  node1 = (struct node *) malloc(sizeof(struct node));
  node2 = (struct node *) malloc(sizeof(struct node));
  node3 = (struct node *) malloc(sizeof(struct node));
  node1-> i = 2;
  root -> next = 
